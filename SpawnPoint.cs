using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPoint : MonoBehaviour
{
    // this script will be filled later with a new spawn point system
    // currently this script's only purpose is to exist so that the object
    // it is attached to can be found
}
